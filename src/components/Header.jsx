import React from 'react';

const Header = (props) => {
  return (
    <div className="header-wrap">
      <div className="link-wrap">
        <a href="#">About</a>
        <a href="#">Projects</a>
        <a href="#">Skills</a>
        <a href="#">Contact</a>
      </div>
    </div>
  );
};

export default Header;
